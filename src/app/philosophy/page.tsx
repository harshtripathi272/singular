import { Philosophy, Manifesto } from "@/components";

export default function PhilosophyPage() {
    return (
        <main className="pt-24 min-h-screen">
            <Philosophy />
            <Manifesto />
        </main>
    );
}
